version = 1
portfolio_id = "engineering-portfolio"

[[projects]]
project_id = "NP01"
name = "Industrial Resilience OT Lab"
relative_path = "../industrial-resilience-ot-lab"
plain_language_description = "A synthetic two-tank industrial cell that detects abnormal signals, enters degraded or fault states, records recovery, and verifies replayable behavior."
domain = "OT / industrial resilience"
prerequisites = ["Python 3.12", "a project-local .venv", "Docker Desktop", "the local OpenPLC image and Mosquitto service described in the project README"]
evidence_location = "var/runs"
latest_valid_location = "var/latest_valid.json"
[projects.actions]
demo = ["scripts/demo.py"]
tests = ["-m", "pytest"]
operate = ["scripts/run_operational.py"]
replay = ["scripts/run_operational.py", "--replay"]
start_services = ["docker", "compose", "up", "-d"]
cleanup = ["docker", "compose", "down", "-v"]

[[projects]]
project_id = "NP02"
name = "Governed Data & Analytics Architecture"
relative_path = "../governed-data-analytics"
plain_language_description = "An imperfect batch is checked against contracts, accepted or quarantined, transformed into trusted analytical views, and reported with quality and lineage."
domain = "data lifecycle / business analytics"
prerequisites = ["Python 3.12", "a project-local .venv", "Docker Desktop", "the isolated PostgreSQL service; run preflight before the pipeline", "Metabase is optional and local-only"]
evidence_location = "var/runs"
latest_valid_location = "var/latest_valid.json"
[projects.actions]
preflight = ["scripts/run_operational.py", "--preflight"]
demo = ["scripts/run_pipeline.py"]
tests = ["-m", "pytest"]
operate = ["scripts/run_operational.py"]
replay = ["scripts/run_operational.py", "--replay"]
start_services = ["docker", "compose", "up", "-d", "postgres", "metabase"]
cleanup = ["docker", "compose", "down", "-v"]

[[projects]]
project_id = "NP03"
name = "Governed AI Assurance"
relative_path = "../governed-ai-assurance"
plain_language_description = "A small synthetic document set is evaluated so every query is supported, abstained, or routed to human review with validated evidence."
domain = "AI assurance / evidence governance"
prerequisites = ["Python 3.12", "a project-local .venv", "the dependencies described in the project README"]
evidence_location = "var/runs"
latest_valid_location = "var/latest_valid.json"
[projects.actions]
demo = ["scripts/demo.py"]
tests = ["-m", "pytest"]
operate = ["scripts/run_operational.py"]
replay = ["scripts/run_operational.py", "--replay"]

[[projects]]
project_id = "NP04"
name = "Architecture Decision Workbench"
relative_path = "../architecture-decision-workbench"
plain_language_description = "Three synthetic architecture options are compared against requirements, assumptions, risks, illustrative costs, and verification needs."
domain = "architecture-to-delivery reasoning"
prerequisites = ["Python 3.12", "a project-local .venv", "pytest for the test suite"]
evidence_location = "var/runs"
latest_valid_location = "var/latest_valid.json"
replay_change = "ASM-002"
[projects.actions]
demo = ["scripts/demo.py"]
tests = ["-m", "pytest"]
operate = ["scripts/run_operational.py"]
replay = ["scripts/run_operational.py", "--replay"]
